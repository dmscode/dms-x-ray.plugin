# xray.json 数据格式规范

> 版本：format 1 · 2026-08-27
> 用途：KOReader `dms-x-ray.koplugin` 划词插件的数据源
> 定位：**独立格式规范**，与任何上游实体数据规范无关。可由任意途径生成（人工 / 脚本 / AI）。

---

## 1. 概述

`xray.json` 是随书携带的「X-Ray 词条数据库」，放在书籍同目录的 sidecar 目录下：

```
<书籍同目录>/<书名去扩展名>.sdr/xray.json
```

例：`银河系搭车客指南5部曲（X-Ray）.epub` → `银河系搭车客指南5部曲（X-Ray）.sdr/xray.json`

读者长按划词时，插件在该文件中查询命中的词条并弹窗展示。**设备端不扫描整书**，所有匹配逻辑基于本文件预构建的索引，保证低算力墨水屏零负担。

设计目标：
- **轻量**：一本中等规模小说（数百实体）通常 < 500KB，内存占用 < 2MB
- **渐进式（非剧透）**：条目可带章节作用范围，首次出现前不可见
- **多义表达**：同一词可对应多条目（如"地球"在不同章节指不同事物）
- **离线**：文件即全部数据，无需联网

---

## 2. 顶层结构

```json
{
  "format": 1,
  "meta": { ... },
  "entries": [ ... ]
}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `format` | 整数 | 是 | 格式版本号，当前为 `1`。插件按版本兼容，不匹配拒绝加载 |
| `meta` | 对象 | 建议 | 书籍元信息（展示与校验用，不影响匹配） |
| `entries` | 数组 | 是 | 实体条目，每项为一个词条（见 §4） |

---

## 3. meta 元数据

```json
"meta": {
  "title": "银河系搭车客指南5部曲",
  "chapter_count": 170,
  "generated_at": "2026-08-27T10:00:00+08:00",
  "source": "AI 阅读 EPUB 生成",
  "generator": "WorkBuddy / 鼠哥"
}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `title` | 字符串 | 建议 | 书名，展示用 |
| `chapter_count` | 整数 | 可选 | 全书章节数（TOC 顶层条目数），用于数据完整性校验 |
| `generated_at` | 字符串 | 可选 | 生成时间（ISO 8601） |
| `source` | 字符串 | 可选 | 数据来源说明 |
| `generator` | 字符串 | 可选 | 生成者 |

> meta 全部字段为可选展示信息，插件不依赖其做匹配。

---

## 4. entries 实体数组

`entries` 是无序数组，每个元素是一个词条。**不使用 key 索引**——同一别名可出现在多条目中（一词多义，见 §6）。

```json
{
  "id": "e001",
  "name": "亚瑟·邓特",
  "type": "characters",
  "aliases": ["亚瑟·邓特", "亚瑟", "邓特"],
  "summary": "地球人，本书主角；地球被毁后踏上银河流浪。",
  "description": "住在英国西南部的地球人……（完整描述）",
  "chapters": [1, 40],
  "stages": [
    { "chapters": [1, 6], "text": "初登场：躺在推土机前抗议。" },
    { "chapters": [7, 40], "text": "登上黄金之心号，成为搭车客。" }
  ],
  "first_appearance": "第1章"
}
```

### 4.1 字段详述

| 字段 | 类型 | 必填 | 语义 |
|---|---|---|---|
| `id` | 字符串 | 是 | 条目唯一 ID（任意，仅内部用，不展示） |
| `name` | 字符串 | 是 | 标准名（展示标题，也参与匹配） |
| `type` | 字符串 | 是 | 实体类型，见 §5 枚举 |
| `aliases` | 字符串数组 | 是 | **全部可匹配形式**（含 name 本身去重）。一个别名可跨条目重复 |
| `summary` | 字符串 | 建议 | 一句话简介（列表/多结果选择页用，建议 ≤80 字） |
| `description` | 字符串 | 可选 | 完整描述（详情页追加显示；数据大时可省略以省内存） |
| `chapters` | `[from, to]` | 可选 | 章节作用范围，见 §7。缺省 = 全书可见 |
| `stages` | 数组 | 可选 | 分阶段描述，见 §7。每个元素 `{chapters, text}` |
| `first_appearance` | 字符串 | 可选 | 首次出现位置（展示用，如"第1章""引子"） |

> **空值处理**：生成时建议把 `null`/空字符串/空数组的字段直接省略，保持数据紧凑。

### 4.2 字段优先级（展示时）

详情弹窗正文按以下优先级取一个文本展示：
1. `stages` 中当前章节命中的 `text`（渐进式剧情跟踪，见 §7）
2. `summary`
3. `description`

若三者都无，弹窗只显示标题。

---

## 5. type 类型枚举

| 值 | 含义 | 展示序 |
|---|---|---|
| `characters` | 人物 | 1 |
| `locations` | 地点 | 2 |
| `terms` | 术语 | 3 |
| `organizations` | 组织 | 4 |
| `events` | 事件 | 5 |

展示序用于多结果排序（人物优先于地点等）。`type` 必须是这五个之一，否则按"术语"兜底。

---

## 6. aliases 别名机制（多义与匹配）

### 6.1 匹配行为

划词时，插件对选中文本做规范化（去首尾标点空白、英文小写）后，与所有条目的 `aliases`（含 `name`）做匹配：
1. **精确命中**：规范化文本等于某别名 → 该条目候选
2. **包含命中**：选词是别名的超集（多划了前后文）或子集（少划了尾巴）→ 候选
3. **模糊命中**（可选开启）：3-gram 剪枝 + 编辑距离 ≤2，用于英文拼写容错

### 6.2 一词多义

**同一别名允许出现在多个条目中**，查询时全部列出（按匹配方式/类型排序）。这是表达"同一词在不同章节指不同事物"的核心机制：

```json
{ "id": "e002", "name": "地球", "aliases": ["地球"], "chapters": [1, 8],  "summary": "亚瑟的母星。" },
{ "id": "e003", "name": "地球（重建版）", "aliases": ["地球", "地球2.0"], "chapters": [38, 40], "summary": "重建的地球。" }
```

划词"地球"：第 1–8 章命中 e002，第 38–40 章命中 e003，中间章节无命中（剧透保护）。两段都可见时（如无 chapters 过滤）则两条都列出。

### 6.3 aliases 生成建议

- 含 `name` 本身及常见称呼（全名/简称/昵称/外文原名）
- 中文书：别名用与正文一致的写法（弯引号、间隔号等要照搬正文形式）
- 英文：小写化由插件处理，数据端写原形即可（"Ford Prefect" 不用写 "ford"）
- 过短或过于通用的词（如"42"可能误配页码）建议不放 aliases，或拆分到独立条目加窄 chapters 限制

---

## 7. 渐进式（非剧透式）标注

这是本格式最重要的特性：**让词条按阅读进度逐步揭示，避免剧透**。两种粒度，可单独或组合使用。

### 7.1 chapters —— 作用范围（条目级）

```json
"chapters": [from, to]
```

- 含义：该条目**仅在第 `from` 章到第 `to` 章（闭区间）可见**
- 章节号：全书 TOC 顶层条目的顺序号，从 1 起
- 缺省：无 `chapters` 字段 = 全书可见

**防剧透语义**：条目"首次出现"之前不可查。例如某反派第 20 章才登场，则其词条 `chapters` 起点设 20，读者在第 5 章划其名时不会命中（不会提前暴露"他是反派"的描述）。

**终点语义**：`to` 通常设为该条目最后一次有意义出现的章节，或全书末章。设为末章表示"出现后一直可见"。若某条目在第 30 章后退出剧情（死亡/离开），`to` 设 30 可让其后不再被查询到（避免读者在结局章看到已退场角色的过时信息）。

**贯穿全书**：主角等从开篇到结尾都在的条目，建议省略 `chapters`（全书可见），而非写 `[1, 末章]`——省略更省内存且语义清晰。

### 7.2 stages —— 分阶段描述（条目内）

同一实体在不同剧情阶段可能有不同身份/状态，用 `stages` 给出分阶段描述，**只显示当前章节命中的那一段**：

```json
"stages": [
  { "chapters": [1, 6],   "text": "初登场：普通地球人，不知银河文明存在。" },
  { "chapters": [7, 25],  "text": "搭车客：地球被毁后随福特漫游银河。" },
  { "chapters": [26, 40], "text": "寻道者：寻找终极问题的答案。" }
]
```

- 每个 stage 也是 `{chapters: [from, to], text}`，语义同 §7.1
- 查询时取**当前章节命中的第一个 stage** 的 `text` 作为展示正文
- 当前章无 stage 命中时，回退到 `summary`/`description`
- `stages` 与条目级 `chapters` 可共存：条目 `chapters` 控制可见性，`stages` 控制各阶段文案

### 7.3 何时用 chapters、何时用 stages

| 场景 | 用法 |
|---|---|
| 角色第 N 章才登场，之前不该被查到 | 条目 `chapters: [N, 末章]` |
| 角色一直都在，但身份在第 M 章发生转变 | 条目无 `chapters` + `stages` 分段 |
| 同一词指代不同事物（地球/重建地球） | 拆成多条目，各自 `chapters` 不重叠 |
| 纯术语，全书含义不变 | 无 `chapters`/`stages`，全书可见 |

### 7.4 章节号约定

- **TOC 顺序号**：以书籍目录（Table of Contents）顶层条目为章节，按文档顺序从 1 编号
- 多卷合集：各卷章节连续编号（卷1第1章=1，卷2第1章=卷1章数+1，依此类推）
- 插件运行时按当前阅读页换算章节号（`ReaderToc` 章节起始页数组二分），与数据端编号对齐即可
- PDF 无稳定章节映射时，插件自动降级为全书模式（忽略 chapters 过滤）

---

## 8. 用 AI 工具生成 xray.json

本格式独立于任何上游规范，**推荐用电脑端 AI 工具阅读书籍本体直接生成**。当前 Trae Work、WorkBuddy、Cursor、Claude 等 AI 编程/对话工具多有免费额度，足够处理一本书，且比人工逐章整理更准确、低成本。

### 8.1 推荐流程

1. **准备书籍文本**：把 EPUB 解压取出 XHTML，或转成纯文本/Markdown（便于 AI 读取；长书可分章分批）
2. **选择 AI 工具**：优先支持长上下文的（Trae Work、WorkBuddy 等可读整本书或大段文本）
3. **按本规范给出 Prompt**（见 §8.2），让 AI 输出符合 `format: 1` 的 JSON
4. **校验**（见 §8.3）后放入 `<书名>.sdr/xray.json`

### 8.2 Prompt 要点

向 AI 下达任务时，要求其：
- 输出严格符合本规范的 JSON（`format: 1` + `meta` + `entries`）
- 提取五类实体（characters/locations/events/terms/organizations）
- 每条目给 `name` + `aliases`（含全名/简称/昵称/外文名，照搬正文写法）
- `summary` 简短（≤80 字）且**不含剧透**——只写该条目首次出现时读者应知的信息
- `chapters` 按"该实体出现的章节范围"（首次出现 → 末次有意义出现），贯穿全书的省略
- 对身份/状态有转变的角色，用 `stages` 分阶段写描述（每段只含该阶段已揭示的信息）
- 同一词指不同事物的，拆成多条目各自 `chapters`
- `description` 可选（详细但不必要的可省略以控制体积）

> 关键：让 AI **站在读者进度视角**写描述——第 N 章的 stage 只能写前 N 章已发生的事，不能剧透后续。

### 8.3 校验清单

- [ ] JSON 合法（可用 `python -m json.tool` 或在线校验）
- [ ] `format` == 1
- [ ] 每条目有 `id`/`name`/`type`/`aliases` 且 `aliases` 非空
- [ ] `type` 为五枚举之一
- [ ] `chapters`/`stages` 的章节号在 `[1, chapter_count]` 范围内
- [ ] `chapters` 的 `from ≤ to`
- [ ] `stages` 各段 `chapters` 区间不重叠（避免同章多义 stage）
- [ ] 体积 < 2MB（超过建议拆分或精简 description）

### 8.4 成本与质量

- 一本 20–50 万字小说，单次 AI 生成通常在免费额度内
- AI 生成后人工抽查 5–10 条核对准确性，比纯人工整理快一个数量级
- 重新生成成本低：书籍修订或想补充条目时，重新跑一次 AI 即可

---

## 9. 完整示例

```json
{
  "format": 1,
  "meta": {
    "title": "银河系搭车客指南5部曲",
    "chapter_count": 170,
    "source": "AI 阅读 EPUB 生成"
  },
  "entries": [
    {
      "id": "e001",
      "name": "亚瑟·邓特",
      "type": "characters",
      "aliases": ["亚瑟·邓特", "亚瑟", "邓特"],
      "summary": "地球人，本书主角。",
      "chapters": [1, 170],
      "stages": [
        { "chapters": [1, 6], "text": "普通地球人，不知银河文明存在。" },
        { "chapters": [7, 170], "text": "搭车客，地球被毁后随福特漫游银河。" }
      ],
      "first_appearance": "第1章"
    },
    {
      "id": "e002",
      "name": "地球",
      "type": "locations",
      "aliases": ["地球"],
      "summary": "亚瑟的母星。",
      "chapters": [1, 8]
    },
    {
      "id": "e003",
      "name": "地球（重建版）",
      "type": "locations",
      "aliases": ["地球", "地球2.0"],
      "summary": "按马文记忆重建的地球。",
      "chapters": [158, 170]
    },
    {
      "id": "e004",
      "name": "无限不可能性引擎",
      "type": "terms",
      "aliases": ["无限不可能性引擎", "不可能性引擎"],
      "summary": "黄金之心号的引擎，穿越概率的关键装置。",
      "chapters": [10, 96]
    },
    {
      "id": "e005",
      "name": "沃贡人",
      "type": "organizations",
      "aliases": ["沃贡人", "沃贡"],
      "summary": "银河系最令人讨厌的种族之一。",
      "description": "以糟糕的诗歌和官僚主义著称。",
      "first_appearance": "第3章"
    }
  ]
}
```

> 上例 e002 与 e003 共用别名"地球"：第 1–8 章命中 e002，第 158–170 章命中 e003，中间章节无命中——这正是渐进式 + 一词多义防剧透的体现。

---

## 10. 版本与兼容

- `format: 1` 为当前版本，插件按此加载
- 未来格式演进会升 `format` 号，插件按版本兼容（不匹配则拒绝加载并提示）
- 字段增减遵循"新增字段向后兼容、删除字段需升版本"原则
